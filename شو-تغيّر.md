# شو ضفت/غيّرت — PrimeAIsion

## 1. مشكلة إنشاء الحساب
الكود صحيح، بس لازم تتأكد من 3 أشياء بـ Firebase Console (مشروع `mohtal-9b1d3`):
1. Authentication → Sign-in method → فعّل **Email/Password** و **Google**.
2. Authentication → Settings → Authorized domains → ضيف الدومين الحقيقي يلي الموقع عليه.
3. جرّب "إنشاء حساب" وافتح Console بالمتصفح (F12) لو استمرت المشكلة، وشوف الـ error code.

## 2. إشعارات حقيقية (Firebase Cloud Messaging)
- `www/assets/firebase-messaging.js` + `www/firebase-messaging-sw.js`: زر "تفعيل الإشعارات" حقيقي، بيسجّل التوكن بقاعدة البيانات (`notificationTokens/{uid}`)، وبيوريّ إشعارات حتى لما التطبيق مسكّر.
- لازم تولّد **VAPID key** من Firebase Console → Cloud Messaging وتحطها بـ `assets/firebase-config.js`.
- بالنسخة أندرويد (Capacitor) الإشعارات بتشتغل native تلقائياً بعد ما تضيف `google-services.json` (تفاصيل بـ README-ANDROID.md).

## 3. شاشة دخول بالصورة يلي بعتها
- `www/assets/splash-login.js` + `.css`: شاشة كاملة الشاشة بالصورة تبعتك (`assets/brand-splash.jpg`) تطلع أول ما حدا يفتح التطبيق، فيها أزرار "تسجيل الدخول" / "إنشاء حساب" / "المتابعة كضيف"، ومربوطة فعلياً بنظام Firebase الموجود (مش نظام تاني).
- بتختفي تلقائياً إذا الشخص مسجّل دخول أصلاً.

## 4. لغات
- `www/assets/i18n.js`: زر 🌐 بيبدّل بين عربي/English/Français لكل النصوص يلي بالقاموس (القائمة، الأزرار، شاشة الدخول، نافذة تسجيل الدخول).
- **مهم بصراحة**: التطبيق الأساسي (`index-BIAuf08k.js`) ملف React مبني ومضغوط سلفاً بدون الكود المصدري، ومصمم RTL بس. الترجمة هون بتشتغل بس على النصوص يلي بالقاموس، والاتجاه (RTL) بيضل ثابت لتجنّب كسر التصميم. لو بدك تعريب/إنجليزي كامل واحترافي 100%، لازم رجوع للمشروع المصدري الأصلي.

## 5. سياسة الخصوصية
- `www/privacy.html`: صفحة كاملة عربي/English فيها زر تبديل لغة، جاهزة تنزل عالدومين وتستخدمها برابط Google Play و Uptodown.
- **لازم تعبّي مكانين محدّدين بالأصفر** (تاريخ التحديث + بريد التواصل) قبل ما تنشرها.

## 6. تحويله لتطبيق أندرويد (Capacitor) + النشر
- كل المشروع اللازم موجود بـ `capacitor.config.json` و `package.json` و `resources/` (أيقونة وسبلاش من صورتك).
- الخطوات الكاملة، بالترتيب، من تثبيت Android Studio لحتى رفع نسخة موقّعة لـ Google Play وUptodown: **README-ANDROID.md**.
- ⚠️ ما فيني ابني ملف APK/AAB فعلي من هون — هيدا لازم يصير بجهازك عندك بـ Android Studio، بس كل الأكواد والإعدادات جاهزة وموصوفة خطوة خطوة.

## نصيحة نشر
1. ارفع محتوى `www/` عالدومين الحقيقي متلما هو موجود حالياً.
2. عبّي `vapidKey` و placeholders سياسة الخصوصية.
3. اتبع README-ANDROID.md لبناء التطبيق.
